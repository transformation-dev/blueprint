name: Coverage on PR

on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master

jobs:
  coverage-set-baseline:
    uses: transformation-dev/blueprint/.github/workflows/coverage.yml@master