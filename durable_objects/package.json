{
  "name": "@transformation-dev/durable_objects",
  "version": "1.0.0",
  "description": "Something",
  "scripts": {
    "publish": "npm run build && npx wrangler publish",
    "build": "npx esbuild src/index.js --outfile=index.mjs --format=esm  --bundle",
    "build:coverage:just-build": "npx esbuild src/index.js --outfile=index.mjs --format=esm  --bundle",
    "build:coverage:dont-call": "npm run build && nyc instrument --es-modules --compact=false index.mjs temp && rm ./index.mjs && mv ./temp/index.mjs ./index.mjs && rmdir ./temp",
    "build:coverage": "",
    "dev": "npx esbuild src/index.js --outfile=index.mjs --format=esm  --bundle --watch"
  },
  "dependencies": {
    "debug": "^4.3.3"
  }
}
